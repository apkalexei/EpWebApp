import { ResourceAllocations } from './resourceAllocations';
export interface ResourceForScheduler {

    id: number;
    name: string;
    //dalsi attributy nezbytne pro detail resource v scheduli
    
}